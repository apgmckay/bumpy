apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bumpy.fullname" . }}-test-connection"
  labels:
    {{- include "bumpy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bumpy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
